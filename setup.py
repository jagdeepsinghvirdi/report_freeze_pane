from setuptools import setup, find_packages

setup(
    name="report_freeze_pane",
    version="0.0.1",
    description="Custom app to enable freeze column dropdown in ERPNext reports",
    author="Your Name",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
